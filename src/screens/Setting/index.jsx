import React from 'react';
import {ScrollView, Text, View} from 'react-native';

const Setting = () => {
  return (
    <ScrollView>
      <View>
        <Text>index</Text>
      </View>
    </ScrollView>
  );
};

export default Setting;
